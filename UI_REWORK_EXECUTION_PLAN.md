# 전투 HUD UI 리워크 실행 계획

## 0. 실행 지시

이 계획은 현재 작업 공간에서 바로 구현한다. 기존 사용자 변경을 보존하며 UI 리워크와 무관한 파일은 수정하지 않는다.

## 1. 목표와 범위

1280×720 기준 전투 화면에 다음 세 HUD 영역을 추가한다.

- 상단: 현재 적용 중인 유물을 보여 줄 유물 슬롯 레일
- 우측: 휠로 선택하고 우클릭으로 사용하는 소모품 슬롯 3칸
- 하단: 현재 콤보의 카드 구성과 사용 순서를 애니메이션으로 보여 주는 카드 레일

유물 시스템 자체와 유물 획득/효과 로직은 이번 범위에 포함하지 않는다. 유물 HUD는 빈 슬롯만 렌더링한다. 카드 실행 순서, 전투 판정, 소모품 효과 등 기존 게임 규칙은 변경하지 않고 HUD가 상태를 관찰하는 방식으로 구현한다.

## 2. 레이아웃 원칙

모든 HUD는 `CanvasLayer` 아래에 배치해 전투 카메라 흔들림과 독립시킨다. 모든 비상호작용 요소는 `mouse_filter = IGNORE`로 설정해 좌클릭 대시와 우클릭 소모품 입력을 가로채지 않게 한다.

### 상단 유물 레일

- 화면 좌우 8% 여백, 우측 메뉴 안전영역 110px을 제외한 폭을 사용한다.
- 높이는 52~60px, 상단 여백은 10~14px로 제한한다.
- 빈 유물 슬롯 8칸을 가로로 배치한다. 슬롯은 42~46px 정사각형을 기본값으로 한다.
- 빈 슬롯은 낮은 명도, 점선/홈 형태 테두리, `EMPTY` 또는 `+` 수준의 약한 표식만 사용한다.
- 유물이 구현되기 전까지 툴팁, 클릭, 드래그, 획득 효과는 만들지 않는다.
- 우측 상단 메뉴 버튼 영역과 겹치지 않도록 컨테이너 최대 폭을 제한한다.

### 우측 소모품 슬롯

- 화면 우측 14~18px 여백, 수직 중앙 부근에 3개 슬롯을 세로로 배치한다.
- 슬롯당 권장 크기는 76×82px, 슬롯 간격은 8px이다.
- 각 슬롯은 번호 `1`, `2`, `3`, 이름 또는 축약명, 남은 개수를 표시한다.
- 선택 상태는 색만 바꾸지 말고 아래 네 가지를 함께 사용한다.
  - 3~4px 고명도 외곽선
  - 좌측 선택 화살표/마커
  - 1.06~1.10 배율 확대
  - 지속적인 약한 글로우/펄스
- 비선택 슬롯은 불투명도와 채도를 낮추되 내용은 읽을 수 있어야 한다.
- 개수가 0이면 흑백 처리, `0` 강조, 선택은 가능하지만 사용 불가 피드백을 짧게 재생한다.
- 마우스 휠 위/아래는 기존 순환 선택을 유지하고, 우클릭은 현재 선택 슬롯만 사용한다.

### 하단 콤보 카드 레일

- 화면 하단 중앙, 좌우 약 18% 여백 안에 배치한다.
- 기본 점유 높이는 112~138px로 제한하고 전투 영역을 덮는 불투명 패널은 사용하지 않는다.
- 배경은 반투명 그라디언트/얇은 테두리만 사용해 투사체와 장판 시인성을 유지한다.
- 카드 권장 크기는 72×96px, 최대 8장까지 보이도록 카드 수에 따라 간격과 배율을 자동 축소한다.
- 카드에는 발동 순서 번호, 카드명, 카드군 색상, 코스트를 표시한다.

## 3. UI 컴포넌트 구조

새 디렉터리 `battle/ui/` 아래에 전투 HUD를 독립 구성한다.

- `BattleHud.gd` / `BattleHud.tscn`
  - 전체 CanvasLayer와 안전영역을 소유한다.
  - 유물, 소모품, 콤보 UI를 조립하고 전투 모델 신호를 연결한다.
- `RelicSlotRail.gd` / `RelicSlotRail.tscn`
  - 빈 슬롯 렌더링만 담당한다.
  - 추후 `set_relics()`를 연결할 수 있는 공개 메서드 시그니처만 마련한다.
- `ConsumableSlotPanel.gd` / `ConsumableSlotPanel.tscn`
  - 3칸 상태, 선택 강조, 개수 변화, 사용 성공/실패 피드백을 담당한다.
- `ComboCardRail.gd` / `ComboCardRail.tscn`
  - 현재 콤보 카드 전체 진입, 활성 카드 드로우, 콤보 교체 애니메이션을 담당한다.
- `ComboCardView.gd` / `ComboCardView.tscn`
  - 카드 한 장의 표시와 상태 전환(`queued`, `active`, `used`)을 담당한다.

현재 `BattleArena._build_hud()`에서 직접 만드는 `ColorRect`/`Label` HUD는 `BattleHud` 인스턴스 생성으로 대체한다. `BattleArena`는 HP, 시간, 처치 수, 콤보 이벤트만 HUD에 전달한다.

## 4. 상태 및 신호 계약

### 소모품 상태

현재 `BattlePlayer`는 소모품을 Dictionary 배열로 보유하고 `consumable_changed(name, count)`만 방출하므로 세 슬롯 전체를 그릴 정보가 부족하다.

후속 구현은 다음 읽기 전용 계약을 추가한다.

- `get_consumable_snapshot() -> Array[Dictionary]`
  - 각 항목: `name`, `kind`, `count`, `slot_index`, `selected`
- `signal consumable_inventory_changed(snapshot: Array, selected_index: int)`
  - 최초 HUD 연결 직후, 휠 선택 변경 후, 우클릭 사용 후 방출
- 기존 `consumable_changed`는 다른 코드 호환을 위해 즉시 제거하지 않는다.

입력과 사용 판정은 계속 `BattlePlayer`가 담당한다. HUD가 직접 `selected_consumable`이나 `consumables` 배열을 변경해서는 안 된다.

### 콤보 상태

현재 `CycleRunner`에는 `card_executed`와 `combo_changed`가 있다. HUD는 다음 규칙으로 연결한다.

- 전투 시작 시 `cycle.combos[0]`을 카드 레일에 즉시 로드한다.
- `combo_changed(next_combo_index)` 수신 시 다음 콤보 전체 진입 애니메이션을 시작한다.
- `card_executed(card, combo_index, card_index)` 수신 시 해당 카드를 활성/사용 상태로 전환한다.
- 정확한 선행 연출이 필요하면 `card_about_to_execute(card, combo_index, card_index)` 신호를 실행 콜백 직전에 추가한다. 게임 실행 타이밍과 `_remaining` 계산은 변경하지 않는다.
- 카드 UI는 `CardInstance`를 복제하거나 게임 상태를 수정하지 않고 표시용 데이터만 추출한다.

## 5. 콤보 카드 애니메이션 명세

### 콤보 진입

1. 새 콤보 카드들은 화면 우측 바깥 `x = viewport_width + 100`에서 시작한다.
2. 전체 카드가 0.22~0.32초 동안 하단 중앙 레일로 함께 이동한다.
3. 카드별 0.025~0.045초 스태거를 적용해 일거에 들어오되 순서를 읽을 수 있게 한다.
4. 이동은 `TRANS_QUINT`, `EASE_OUT`; 과한 바운스와 화면 중앙 침범은 피한다.
5. 이전 콤보는 0.12~0.18초 동안 아래쪽으로 20px 이동하며 페이드아웃한다.

### 카드 사용

1. 사용 직전/사용 프레임에 해당 카드가 레일에서 위로 18~26px 올라오고 1.10~1.16배 확대된다.
2. 카드 테두리와 카드군 색상이 0.08~0.12초 발광한다.
3. 사용 완료 후 카드는 원위치로 복귀하며 채도와 불투명도가 낮아져 `used` 상태가 된다.
4. 다음 카드는 약한 대기 펄스로 곧 사용될 카드임을 예고한다.
5. 마지막 카드까지 사용되면 다음 콤보 진입 애니메이션으로 자연스럽게 연결한다.

### 가독성과 성능

- 애니메이션은 하단 20% 안에서 끝나야 하며 플레이어 중심과 적 공격 텔레그래프를 가리지 않는다.
- 한 카드 이벤트마다 Tween 하나를 재사용/정리하고, 종료된 UI 노드와 Tween을 누적시키지 않는다.
- 전투 종료 시 모든 Tween을 중단하고 HUD를 안전하게 해제한다.
- 화면 흔들림은 전투 월드에만 적용하고 CanvasLayer HUD에는 적용하지 않는다.

## 6. 구현 순서

### Phase A — 기준선 확보

- 기존 단위 테스트와 통합 테스트를 실행해 기준선 기록
- 1280×720 현재 전투 HUD 스크린샷 확보

### Phase B — HUD 골격

- `BattleHud` CanvasLayer와 안전영역 구성
- 기존 HP/시간/처치 정보 이전
- 상단 빈 유물 슬롯과 우측 3칸 소모품 슬롯 정적 배치
- 모든 HUD의 마우스 필터가 전투 클릭 입력을 막지 않는지 확인

### Phase C — 소모품 상태 연동

- `BattlePlayer`에 스냅샷/전체 상태 신호 추가
- 휠 순환 선택과 선택 강조 연결
- 우클릭 사용 후 개수/비활성 상태 갱신
- 0개 슬롯 사용 시 실패 피드백 구현

### Phase D — 콤보 레일과 애니메이션

- 현재 콤보 카드 뷰 생성
- 전투 시작 콤보 0 진입 연출
- 카드 실행별 활성/사용 상태 연출
- 콤보 변경 시 우측 진입/기존 카드 퇴장 연출
- 1장, 3장, 8장 조합에서 자동 폭 조절

### Phase E — 시각 QA와 마감

- 카드군별 색상과 선택 상태 대비 조정
- 밝은 배경/어두운 배경 모두에서 텍스트 가독성 확인
- 전투 중심, 적 텔레그래프, 우측 소모품과 하단 카드가 서로 겹치지 않는지 확인
- 1280×720 기준 스크린샷을 첨부 이미지와 비교

### Phase F — 검증과 빌드

- 전체 단위·회귀 테스트 실행
- UI 통합 테스트 실행
- 메인 씬 부팅과 20초 이상 전투 시뮬레이션
- Windows EXE 재빌드 및 실행 확인
- 변경 파일이 HUD 작업 범위에 맞는지 검토

## 7. 테스트 추가 항목

`tests/integration_runner.gd` 또는 별도 `tests/battle_hud_runner.gd`에 아래 검증을 추가한다.

- `BattleHud`가 CanvasLayer로 생성되고 전투 월드 흔들림과 분리되는지
- 유물 슬롯이 정확히 지정 개수만큼 빈 상태로 표시되는지
- 소모품 슬롯이 항상 3칸이며 선택 인덱스가 한 칸에만 적용되는지
- 휠 위/아래 이벤트가 `0 → 1 → 2 → 0` 및 역방향으로 순환하는지
- 우클릭 사용 뒤 해당 슬롯 개수만 감소하고 선택 상태가 유지되는지
- HUD Control 위 좌클릭도 플레이어 대시를 방해하지 않는지
- 첫 콤보가 전투 시작 시 로드되는지
- `card_executed`의 `combo_index/card_index`와 활성 카드 뷰가 일치하는지
- `combo_changed` 후 카드 레일의 데이터가 다음 콤보로 교체되는지
- 콤보 8장에서도 카드가 화면 우측 소모품 슬롯을 침범하지 않는지
- 전투 종료 후 Tween/카드 뷰가 남지 않는지

## 8. 완료 조건

- 첨부 이미지의 정보 구조인 상단 유물, 우측 소모품 3칸, 하단 현재 콤보 레일이 1280×720 전투 화면에 구현되어 있다.
- 유물 슬롯은 비어 있지만 의도된 자리와 슬롯 형태가 명확하다.
- 휠 조작 시 현재 소모품이 색상 외에도 외곽선, 마커, 배율로 즉시 구분된다.
- 우클릭 사용 결과와 잔여 개수가 같은 프레임 또는 다음 프레임에 반영된다.
- 현재 콤보 전체가 우측에서 하단 중앙으로 진입하고, 카드 사용 순서에 맞춰 한 장씩 활성화된다.
- UI 때문에 좌클릭 대시, 우클릭 소모품, 적 텔레그래프 가독성이 손상되지 않는다.
- 기존 전투 규칙과 콤보 실행 순서에는 회귀가 없다.
- 자동 테스트, 전투 시뮬레이션, Windows EXE 부팅 검증이 모두 통과한다.

## 9. 명시적 비범위

- 유물 데이터 모델, 획득, 장착, 효과 계산
- 소모품 신규 종류나 효과 밸런스 변경
- 콤보 편집 규칙과 카드 실행 주기 변경
- 전투 배경, 캐릭터, 적 애니메이션 재작업
- 메인 메뉴와 비전투 화면의 전면 리디자인
