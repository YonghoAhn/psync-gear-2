# Carbo 게임 구현 재작업 계획

## 1. 계획의 목적과 기준

이 문서는 `game_description` 안의 다음 11개 기획 문서를 기준으로 현재 GDScript 구현을 재작업하기 위한 실행 계획이다.

- A. 게임 플레이 요약
- A. 시작 및 설정 UI
- A. 일반형 캐릭터
- A. 전투 시스템
- B. 게임 흐름도
- B. 기믹형 캐릭터
- B. 인게임 UI
- B. 캐릭터 시스템
- C. 카드 및 세트 시스템
- D. 적/보스 시스템
- E. 맵/스테이지 시스템

구현 판단 우선순위는 다음과 같이 둔다.

1. 더 최근에 갱신된 구체적인 규칙
2. 상태가 `완료`인 시스템 문서
3. 여러 문서에서 반복되는 공통 규칙
4. 미정인 항목은 데이터/정책 객체로 분리하여 나중에 바꿀 수 있게 구현

이 우선순위에 따라 전투 입력과 카드 실행은 2026-08-05의 `A. 전투 시스템`을 우선한다. 즉, 좌클릭은 회피, 우클릭은 소모품 사용, 휠은 소모품 선택이며, 카드는 수동 2슬롯 시전이 아니라 사전에 편성한 콤보/사이클을 자동 실행한다.

## 2. 현재 구현 진단

### 2.1 재사용 가능한 기반

- `CardDef`, `CardInstance`, `EffectSpec`, `TargetSpec`, `ConditionSpec`의 데이터 주도형 효과 정의 방식
- `EffectExecutor`의 투사체, 범위 공격, 장판, 설치물, 소환, 상태이상 실행 개념
- `EntityBase`와 체력, 이동, 상태이상, 피해 수신 컴포넌트 분리 개념
- `EnemyDef`의 Resource 기반 적 데이터 정의 방식
- `GlobalEventBus`의 시스템 간 이벤트 경계
- `GameApp`과 `SceneRouter`의 상태 전환 책임 분리 개념

### 2.2 교체 또는 대규모 수정 대상

- `CardDrawService`, `HandSlotController`, `CardCastController`: 2슬롯 수동 드로우 방식이 최신 자동 사이클 기획과 충돌
- `DeckController`: 카드 보관은 살릴 수 있으나 콤보 편성, 전체 카드 배치 검증, 세트 계산, 런 중 강화가 없음
- `PlayerController`: 좌/우클릭을 카드 시전에 사용하고 Space/Shift를 회피에 사용하여 최신 조작과 반대
- `CombatStatsComponent`, `DamageReceiver`: 기획 스탯이 누락되고 방어력을 고정 감산하여 명시된 피해 공식과 불일치
- `CharacterDef`: 고유 특성 모듈, 시작 카드군 제한, 해금 조건이 없음
- `EnemyDef`/AI: 잡몹 5유형, 네임드 4유형, 조립형 보스 패턴을 표현할 수 없음
- `GameApp`: 맵 선택, 노드 선택, 보상, 덱 정제, 보스 클리어, 무한 모드 흐름이 없음

### 2.3 즉시 해결해야 할 실행 차단 요소

- `project.godot`, 실행 씬, UI/스테이지 씬, 테스트가 없음
- `entities/GameApp.gd`가 실제로는 `StatusApplier` 복제본이며 `entities/StatusApplier.gd`와 같은 `class_name`을 선언
- `SceneRouter`가 존재하지 않는 씬 경로만 참조
- `EnemyAIBase`가 별도 파일 내부 클래스에 직접 의존하여 로드 계약이 불명확
- 전역 RNG와 개별 RNG가 혼용되어 동일 seed 재현성이 보장되지 않음
- 현재 폴더는 Git 저장소가 아니므로 단계별 롤백 지점이 없음

## 3. 목표 런 흐름

```text
메인 메뉴/설정
  -> 맵 선택
  -> 캐릭터 선택
  -> 허용된 시작 카드군 선택
  -> 초기 덱의 콤보/사이클 확인
  -> 노드 맵 진입
  -> 3개 후보 중 다음 노드 선택
  -> 노드 실행
       전투: 제한 시간 생존
       엘리트: 네임드 전투
       상점: 구매/제거
       휴식: 회복/강화/제거 또는 정제
       이벤트: 선택지와 결과
       보스: 조립형 보스 전투
  -> 카드 보상 3장 중 1장 선택 또는 스킵
  -> 덱 변경 시 모든 카드를 콤보에 재배치하고 코스트 검증
  -> 보스 처치
       일반 모드: 런 클리어
       해금된 무한 모드: 난이도 스케일 후 다음 보스 사이클
```

## 4. 목표 아키텍처

### 4.1 런 계층

- `RunSession`: seed, 선택 맵/캐릭터/시작 카드군, 덱, 재화, 유물, 소모품, 현재 노드, 모드, 결과를 소유
- `RunFlowController`: 화면/노드 상태 전이와 승패 판정
- `MapDef`, `RunMap`, `MapNode`: 최대 노드 수, 노드 후보 3개, 경로, 마지막 보스 보장
- `NodeEncounterController`: 전투/엘리트/상점/휴식/이벤트/보스를 공통 노드 결과로 정규화
- `RunRng`: 맵 생성, 보상, 전투 스폰을 이름이 붙은 RNG 스트림으로 분리

### 4.2 카드/콤보 계층

- `DeckState`: 런의 모든 `CardInstance`를 단일 소유
- `ComboDef`/`ComboState`: 카드 순서와 콤보 코스트 한도를 소유
- `CycleState`: 콤보 순서와 현재 실행 위치를 소유
- `ComboValidator`: 모든 덱 카드가 정확히 한 번 배치되었는지, 빈 참조가 없는지, 각 콤보가 코스트 한도 이내인지 검증
- `CycleRunner`: 완성된 콤보들을 순차 자동 실행하고 마지막 콤보 뒤 처음으로 복귀
- `SynergyResolver`: 같은 카드군 연속 배치 시 콤보 시너지, 보조/중립 예외, 덱 전체 카드군 수/레벨 기반 세트 효과 계산
- `RewardOfferService`: 현재 덱과 같은 카드/카드군에 가중치를 주되 seed로 재현 가능한 3장 보상 생성
- `CardUpgradeService`: 개별 카드 +1/+2/+3/각성 및 카드군 보유량 기반 세트 단계 처리

### 4.3 전투 계층

- `CombatStats`: 최대/현재 체력, 이동 속도, 마력, 마공학 기술력, 상태이상 위력, 치명타 확률/피해, 공격 속도, 방어력, 속성 저항, 보호막/회복량, 강인함, 행운
- `DamageContext`: 기본 피해, 카드 계수, 피해 속성, 치명타, 피해 증가, 난이도, 공격자/대상 정보를 한 번만 계산
- `DamagePipeline`: 문서의 곱연산 순서와 `100 / (100 + 방어력)`, `1 - 저항률`을 적용
- `StatusDef`/`StatusRuntime`: 피해형/제어형/약화형/축적형 분류, 면역, 강인함 기반 지속시간/중첩 감소, 시간 만료
- `DodgeController`: 좌클릭 방향 회피, 쿨다운, 무적 시간
- `ConsumableController`: 우클릭 사용, 휠 선택, 보유 수량과 HUD 이벤트
- `ReactionResolver`: 물 장판+전기 등 카드군 간 반응을 데이터로 정의

### 4.4 캐릭터 계층

- `CharacterDef`: 기본 스탯, 허용 시작 카드군, 해금 조건, 특성 모듈 목록
- `CharacterTrait`: 카드군 영향형, 스탯 영향형, 유물/노드 영향형, 특수 규칙형 공통 인터페이스
- 일반형 캐릭터: 수치와 카드군 보정만으로 구성 가능한 데이터 중심 프리셋
- 기믹형 캐릭터: 이벤트 훅과 규칙 변경 모듈을 조합하는 프리셋
- `UnlockProfile`: 캐릭터/카드군/맵/무한 모드 해금 상태와 도전과제 진행 저장

### 4.5 적/보스 계층

- 잡몹 행동 모듈: 근접, 원거리, 지원, 소환, 방해
- 네임드 패턴 모듈: 단일 압박, 소환, 이동, 상태이상
- `BossDef`: 페이즈와 여러 `BossPatternDef` 조합
- `BossPatternRunner`: 쿨다운, 우선순위, 동시 실행 금지, 전조, 실행, 회복 상태를 관리
- `EncounterDef`/`SpawnDirector`: 노드 종류, 시간, 난이도, 적 풀, 스폰 곡선, 목표를 데이터로 정의

### 4.6 UI 계층

- 시작 UI: 메인 메뉴, 설정, 맵 선택, 캐릭터 선택, 시작 카드군 선택
- 런 UI: 노드 맵, 카드 보상, 콤보 편집, 상점, 휴식, 이벤트, 결과
- 전투 HUD: 체력/보호막, 회피 쿨다운, 소모품/선택 상태, 남은 시간/목표, 현재 콤보와 카드, 다음 콤보, 보스 체력/페이즈, 상태이상
- 비어 있는 UI 문서 때문에 1차 구현은 기능 중심 와이어프레임으로 한정하고, 시각 스타일은 별도 기획 승인 뒤 진행

## 5. 단계별 subtask 실행 계획

각 단계는 앞 단계의 완료 조건을 통과해야 시작한다. 동일 단계 안에서 파일 소유권이 겹치지 않는 작업만 병렬화한다.

### Phase 0. 실행 가능한 기준선 만들기

#### ST-00 프로젝트 부트스트랩

- `project.godot`, `Main.tscn`, Autoload, 입력 액션, 기본 폴더 구조 생성
- 기존 스크립트를 실제 씬 트리에 연결할 최소 플레이그라운드 작성
- 완료 조건: 헤드리스 프로젝트 로드와 빈 메인 씬 실행 성공

#### ST-01 파서/클래스 결함 정리

- 중복 `StatusApplier` 파일 제거 또는 올바른 이름으로 교정
- AI 타입 파일 로드 구조 명시화
- 필수/선택 노드 참조를 안전하게 정리
- 완료 조건: 모든 `.gd` 파싱 성공, 중복 `class_name` 0건

#### ST-02 테스트 하네스와 결정적 RNG

- GDScript 단위 테스트 또는 최소 헤드리스 테스트 러너 구성
- `RunRng` 도입 및 전역 `randf/randi` 직접 호출 제거 계획 적용
- 완료 조건: 같은 seed의 맵/보상 결과가 반복 실행에서 동일

### Phase 1. 데이터 계약과 런 상태

#### ST-10 공통 ID/레지스트리

- 카드, 카드군, 캐릭터, 적, 맵, 노드, 상태이상 Resource의 ID 검증과 조회 레지스트리 구현
- 완료 조건: 중복/누락 ID를 시작 시 오류로 보고

#### ST-11 RunSession/저장 계약

- Dictionary 중심 `run_data`를 타입이 명확한 런 상태 객체로 교체
- 런 저장과 영구 해금 저장을 분리
- 완료 조건: 런 상태 직렬화 왕복 테스트 통과

#### ST-12 게임 상태 머신 재구축

- BOOT, MAIN_MENU, MAP_SELECT, CHARACTER_SELECT, STARTING_FAMILY_SELECT, RUN_MAP, ENCOUNTER, REWARD, DECK_EDIT, SHOP, REST, EVENT, RESULT 상태 정의
- 잘못된 전이 방지와 전이 이벤트 구현
- 완료 조건: 정상 클리어/사망/포기 흐름 상태 테스트 통과

### Phase 2. 카드, 콤보, 사이클 핵심 교체

#### ST-20 카드 스키마 개편

- 공격/방어/유틸 역할, 즉발/투사체/설치·소환/상태이상/이동 유형, 대/소/카드군 분류, 일반~전설 5단계 희귀도 반영
- 콤보 편성 코스트와 자동 실행 간격을 런타임 MP와 분리
- 완료 조건: 카드 정의 검증 테스트 및 샘플 카드 로드 성공

#### ST-21 덱 단일 소유권과 콤보 편집

- 기존 draw/used/2슬롯 모델 제거
- 모든 카드 정확히 1회 배치, 무제한 카드 수, 콤보 코스트 제한 검증
- 완료 조건: 누락/중복/초과 코스트를 각각 차단하는 테스트 통과

#### ST-22 자동 CycleRunner

- 콤보 A -> B -> C -> A 반복, 콤보 내부 카드 순차 실행, 일시정지/전투 종료 시 안전 중단
- 공격 속도 적용 지점을 카드 반복/소환체 공격 간격과 분리해 명시
- 완료 조건: 고정 seed/고정 delta 시 실행 순서와 횟수가 예상값과 일치

#### ST-23 콤보 시너지와 세트 효과

- 같은 카드군 인접/연속 규칙, 서로 다른 카드군 시너지 미발동, 보조/중립 예외 구현
- 카드군 보유 수 또는 카드 레벨 기반 런 전체 세트 효과 구현
- 완료 조건: 단일/혼합/중립 삽입 조합 표 기반 테스트 통과

#### ST-24 강화/제거/보상 가중치

- 개별 강화 A안 + 카드군 시너지 B안 혼합 모델
- 전투/엘리트/상점/이벤트/보스 보상 3장, 스킵, 덱 기반 가중치
- 상점과 휴식의 제거 경로 및 제거 불가 카드 처리
- 완료 조건: 10,000회 seed 표본에서 가중치 방향성 확인 및 모든 보상 경로 테스트 통과

### Phase 3. 전투 공식과 플레이어 조작

#### ST-30 스탯 모델 교체

- 문서의 기본/공격/방어/특수 스탯 전체 반영
- 캐릭터/적/버프/세트 효과가 같은 모디파이어 파이프라인 사용
- 완료 조건: 각 스탯의 flat/percent 조합 테스트 통과

#### ST-31 피해 계산 파이프라인

- 치명타를 한 번만 판정하고 명시된 계산 순서를 적용
- 물리/마법/마공학/순수 피해, 방어/저항/난이도 보정 구현
- 보호막 선소모와 보호막 방어/저항 정책 분리
- 완료 조건: 기획 예제 수치에 대한 골든 테스트 통과

#### ST-32 상태이상/반응

- 4개 상태이상 분류, 면역/저항/강인함, 지속시간/중첩, 제거 규칙 구현
- 핵심 속성 반응을 `ReactionDef` 데이터로 분리
- 완료 조건: 적용, 저항, 면역, 만료, 축적 발동 테스트 통과

#### ST-33 조작 체계 교체

- WASD 이동, 마우스 좌표 조준, 좌클릭 회피, 우클릭 소모품, 휠 선택
- 기존 마우스 카드 시전과 R 리롤 제거
- 완료 조건: 입력 이벤트 통합 테스트와 회피 무적/쿨다운 테스트 통과

#### ST-34 효과 실행기 정합화

- 기존 `EffectExecutor`, 투사체, 장판, 설치물에 `DamageContext`와 세트/특성 컨텍스트 주입
- 지연 실행이 전투 종료 후 남지 않도록 취소 토큰 적용
- 완료 조건: 주요 카드 타입별 전투 통합 테스트 통과

### Phase 4. 캐릭터와 해금

#### ST-40 캐릭터 특성 모듈

- 4종 고유 특성 유형을 이벤트 훅/모디파이어로 구현
- 일반형은 데이터 조합만으로 생성 가능하게 구성
- 완료 조건: 샘플 일반형 캐릭터가 코드 추가 없이 Resource만으로 동작

#### ST-41 기믹형 확장 지점

- 카드 실행 전후, 회피, 노드 진입/완료, 보상 생성, 유물 획득 등 제한된 훅 제공
- 기믹 모듈이 기본 시스템을 직접 참조하지 않도록 컨텍스트 API 제공
- 완료 조건: 샘플 기믹형 캐릭터 1종의 규칙 변경 테스트 통과

#### ST-42 시작 카드군과 해금

- 캐릭터별 선택 가능 카드군 제한
- 클리어/카드군/플레이 스타일/난이도/수집/특수 조건 도전과제 평가
- 완료 조건: 잠김/해금/영구 저장/재실행 복원 테스트 통과

### Phase 5. 적, 네임드, 조립형 보스

#### ST-50 행동 모듈 기반 잡몹 5종

- 이동, 공격, 지원, 소환, 방해 행동을 조합 가능하게 분리
- 완료 조건: 5개 역할의 최소 샘플이 전투 플레이그라운드에서 동작

#### ST-51 네임드 4종

- 단일 압박/소환/이동/상태이상 패턴과 전조 표시 구현
- 완료 조건: 각 네임드가 의도한 빌드 축을 검증하는 통합 테스트 통과

#### ST-52 조립형 보스

- 페이즈, 패턴 풀, 전조, 쿨다운, 패턴 충돌 규칙, 특수 실패 조건 구현
- 완료 조건: 패턴 조합만 바꿔 서로 다른 보스 2종 생성 가능

### Phase 6. 맵/스테이지와 노드

#### ST-60 노드 맵 생성

- 맵별 최대 노드 수, 매 단계 최대 3개 후보, 경로 연결, 마지막 보스 보장
- 다각형/원형 전장 메타데이터 및 seed 재현
- 완료 조건: 도달 불가 노드/보스 없는 맵/최대 노드 초과가 0건

#### ST-61 전투/엘리트/보스 노드

- 전투 제한 시간 생존, 네임드, 보스 목표와 승패 처리
- 완료 조건: 생존 성공, 체력 0, 특수 기믹 실패, 포기 시나리오 통과

#### ST-62 상점/휴식/이벤트

- 구매, 제거, 회복, 강화/정제, 이벤트 선택지와 결과 구현
- 완료 조건: 재화 원자성, 불가능한 구매/제거 차단, 노드 결과 저장 테스트 통과

#### ST-63 보상과 다음 노드 연결

- 노드 완료 -> 보상 -> 덱 재편성 -> 맵 복귀 흐름
- 완료 조건: 덱 변경 후 유효한 콤보가 없으면 다음 전투 진입 차단

#### ST-64 클리어/무한 모드

- 보스 처치 클리어, 맵별 무한 모드 해금, 이후 보스 반복과 난이도 스케일
- 완료 조건: 최초 클리어 전 무한 모드 잠금 및 클리어 후 해금 테스트 통과

### Phase 7. UI 기능 완성

#### ST-70 시작/설정 UI

- 메인 메뉴, 옵션 저장, 맵/캐릭터/시작 카드군 선택, 잠금 표시
- 완료 조건: 키보드/마우스로 새 런 시작 가능

#### ST-71 인게임 HUD

- 체력, 보호막, 회피, 소모품, 시간/목표, 콤보/카드 진행, 상태이상, 보스 정보
- 완료 조건: 이벤트 기반 갱신이며 프레임 폴링 의존 최소화

#### ST-72 노드/보상/덱 편집 UI

- 3개 노드 선택, 카드 3장/스킵, 드래그 기반 콤보 편집과 오류 표시
- 완료 조건: 유효하지 않은 사이클 확정 불가, 오류 이유를 사용자에게 표시

#### ST-73 상점/휴식/이벤트/결과 UI

- 각 노드 행동, 승리/패배/포기, 해금 결과 표시
- 완료 조건: 전체 런을 디버그 명령 없이 UI만으로 완료 가능

### Phase 8. 콘텐츠 수직 슬라이스와 안정화

#### ST-80 최소 콘텐츠 팩

- 맵 1개, 일반형/기믹형 캐릭터 각 1개, 시작 카드군 2개, 카드군당 핵심 카드, 잡몹 5종, 네임드 2종 이상, 조립형 보스 1종
- 완료 조건: 15~30분 분량의 한 런 완주 가능

#### ST-81 밸런스/텔레메트리

- 카드 선택률, 콤보 실패 원인, 피해원, 사망원인, 노드 선택률을 로컬 로그로 기록
- 완료 조건: seed와 빌드를 포함한 런 리포트 생성

#### ST-82 회귀/성능/저장 호환

- 헤드리스 회귀 테스트, 다수 적/투사체 성능, 저장 버전 마이그레이션
- 완료 조건: 목표 적 수에서 프레임/메모리 기준 통과, 손상 저장 안전 실패

## 6. 의존성과 병렬 실행 묶음

```text
Phase 0
  -> Phase 1
      -> Phase 2 카드 핵심
      -> Phase 3 전투 핵심
          -> Phase 4 캐릭터
          -> Phase 5 적/보스
      -> Phase 6 맵/노드
          -> Phase 7 UI
              -> Phase 8 수직 슬라이스/안정화
```

- Phase 0은 전 작업의 선행 조건이다.
- Phase 1 완료 후 Phase 2의 데이터 스키마와 Phase 3의 기초 스탯 모델은 병렬 가능하다.
- Phase 4와 Phase 5는 Phase 2/3의 이벤트 및 전투 계약이 고정된 뒤 병렬 가능하다.
- Phase 7의 시각 작업은 기능 계약이 자주 바뀌는 Phase 2~6보다 늦게 시작하되, 와이어프레임 HUD는 Phase 3 통합 검증용으로 먼저 둘 수 있다.
- 각 subtask는 데이터/런타임/UI 파일 소유자를 분리하고, 공유 계약 변경은 별도 선행 subtask로 처리한다.

## 7. 검증용 대표 시나리오

1. 새 프로필에서 잠금 해제된 맵/캐릭터/카드군만 선택할 수 있다.
2. 시작 덱의 모든 카드는 정확히 한 콤보에 들어가며 초과 코스트 콤보는 저장할 수 없다.
3. 전투 진입 후 입력 없이도 콤보 A/B/C가 순서대로 반복된다.
4. 같은 카드군 연속 카드만 콤보 시너지를 만들고 중립 카드는 연계를 끊지 않는다.
5. 좌클릭 회피 중 무적이 적용되고 우클릭은 현재 선택한 소모품만 사용한다.
6. 피해 계산이 기획 공식과 일치하고 순수 피해는 방어/저항을 무시한다.
7. 전투 노드는 제한 시간 생존 시 성공하고 체력 0/특수 기믹 실패/포기는 실패한다.
8. 노드 완료 후 카드 3장 중 1장 또는 스킵이 가능하고 현재 덱 카드군이 더 자주 나온다.
9. 보스는 여러 패턴과 페이즈를 조합하며 처치 시 일반 런이 종료된다.
10. 맵 최초 클리어 후에만 무한 모드가 열리고 다음 보스 사이클의 난이도가 증가한다.

## 8. 구현 전 확정하거나 정책 객체로 격리할 항목

다음은 문서끼리 충돌하거나 아직 내용이 비어 있으므로 코드에 하드코딩하지 않는다.

1. 시작 순서: 게임 플레이 요약은 `맵 -> 캐릭터 -> 카드군`, 게임 흐름도는 `캐릭터 -> 카드군`만 기술한다. 이 계획은 더 완전한 전자를 기본값으로 사용한다.
2. 맵 선택 방식: 플레이어 선택과 자동 배정 문구가 함께 있다. `MapSelectionPolicy`로 분리하고 기본은 선택으로 둔다.
3. 카드 조작: 카드 문서의 QWERT 수동 연계와 최신 전투 문서의 자동 사이클이 충돌한다. 기본은 자동 사이클이며 QWERT 규칙은 채택하지 않는다.
4. 콤보 코스트: 편성 한도인지 런타임 자원인지 명확하지 않다. 기본은 편성 한도이며 기존 MP는 제거/선택형 특수 자원으로 격리한다.
5. 카드 제거: 후보안이 확정되지 않았지만 맵 문서상 상점에 제거 기능이 있다. 기본은 휴식+상점 제거로 둔다.
6. 보호막의 방어/저항 감소 문구: 보호막에 적용되는 방어 효율 계수로 정책화하고 정확한 수치는 밸런스 데이터로 둔다.
7. 전투 시간: 생존 타이머 성공과 제한 시간 초과 패배가 함께 있다. 노드별 `ObjectiveDef`가 성공/실패 의미를 결정하게 한다.
8. 일반형/기믹형 캐릭터와 UI 두 문서는 템플릿만 있고 구체 요구가 없다. 기능 최소안만 구현하고 콘텐츠/비주얼 확장은 별도 승인 대상으로 둔다.

## 9. 단계별 완료 보고 형식

각 subtask 완료 시 다음을 남긴다.

- 변경한 계약과 파일
- 기획 요구사항과의 추적 링크 또는 항목
- 자동 테스트 결과와 수동 확인 절차
- 남은 미정 사항과 임시 정책
- 다음 subtask가 의존할 공개 API

코드 재작업은 Phase 0부터 시작하며, Phase 2 이전에는 콘텐츠 대량 입력을 하지 않는다. 먼저 데이터 계약과 실행 루프를 고정해야 이후 카드/적/맵 콘텐츠를 다시 만드는 일을 피할 수 있다.
